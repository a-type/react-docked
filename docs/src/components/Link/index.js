import styled from 'styled-components';

const ThemeLink = styled.a`
  color: var(--color-secondary);
`;

ThemeLink.Clear = styled.a`
  color: inherit;
  text-decoration: none;
`;

export default ThemeLink;
